name: daydart_flutter
description: With the same Day. JS API time processing library, convenient to do DateTime operation.
version: 1.0.1
homepage: https://github.com/supermebing/daydart_flutter
repository: https://github.com/supermebing/daydart_flutter
environment:
  sdk: ">=3.1.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
